services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=page-summary-database'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=maciek'
    ports:
      - '15432:5432'
